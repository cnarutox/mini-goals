<!--pages/add_habit/add_habit.wxml-->
<button class="btn1">
  <image class="btnImg" src="/pages/images/icon_nav_feedback.png"></image>
  <view>自定义习惯</view>
</button>
<button class="btn2">
  <image class="btnImg" src="/pages/images/icon_nav_feedback.png"></image>
  <view>搜索习惯</view>
</button>
<view class="box">
  <view>
    <view class="row" wx:for="{{buttonFirstLine}}" wx:for-item="item">
      <button class="cell" type="default" size="mini" bindtap="goIndex">{{item.title}}</button>
    </view>
  </view>
  <view>
    <view class="row" wx:for="{{buttonSecondLine}}" wx:for-item="item">
      <button class="cell" type="default" size="mini" bindtap="goIndex">{{item.title}}</button>
    </view>
  </view>
</view>
<!-- <view id="panel" class="flex-column">
  <view class="box">
    <view>
      <view class="row" wx:for="{{buttonFirstLine}}" wx:for-item="item">
        <button class="cell" type="default" size="mini" bindtap="goIndex">{{item.title}}</button>
      </view>
    </view>
    <view>
      <view class="row" wx:for="{{buttonSecondLine}}" wx:for-item="item">
        <button class="cell" type="default" size="mini" bindtap="goIndex">{{item.title}}</button>
      </view>
    </view>
  </view>
</view> -->
